name: Security Scan

on:
  pull_request:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions: 
  contents: read
  security-events: write
  
jobs:
  semgrep:
    uses: vedanshvachani/secure-ci-templates/.github/workflows/semgrep.yml@main


